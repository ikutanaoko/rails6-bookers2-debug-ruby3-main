<% if search_book == "日付を選択してください" %>
  日付を選択してください
<% else %>  

  <div><%= create_at %> の投稿件数： <%= search_book_count %></div>
  <ul>
    <% search_book.each do |book| %>
      <li><%= link_to book.title, book_path(book) %></li>
    <% end %>
  </ul>
  
<% end %>
